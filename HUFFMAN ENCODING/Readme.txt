Whatever bmp file you need to compress. First name it input.bmp. We have made our code which runs for 32-bit depth of bmp image so kindly check whether it is 32-bit bmp imge.


The code produces two outputs - "output.bin" and "huffman_tree.txt".
